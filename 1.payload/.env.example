# coinBank server (payload reads these for WebSocket connection)
SERVER_IP=localhost
SERVER_PORT=8443
SERVER_WSS=true

# Optional: full WebSocket URL (overrides SERVER_IP/PORT/WSS if set)
# COINBANK_WS_URL=wss://your-server:8443

# Optional: payload UID (if not set, derived from this PC's MAC address)
# UID=my-machine-001
