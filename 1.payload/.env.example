# coinBank server (payload reads these for WebSocket connection; backend uses HTTP/WS)
SERVER_IP=localhost
SERVER_PORT=8080
SERVER_WSS=false

# Optional: full WebSocket URL (overrides SERVER_IP/PORT/WSS if set)
# COINBANK_WS_URL=ws://your-server:8080

# Optional: payload UID (if not set, derived from this PC's MAC address)
# UID=my-machine-001
